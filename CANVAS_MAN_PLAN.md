# Canvas Man Website – Joomla to Static Site Conversion Plan

## Overview

Convert the Joomla-driven website at https://www.canvasman.com into a fully static website built with **Eleventy (11ty)**. The local `site_files/` folder contains the original Joomla install (tarball) and database. The goal: a static portfolio site that's easy to update — business info, pages, and gallery images all editable from simple files in git.

## Stack & hosting

- **Generator:** Eleventy 3.x
- **Image processing:** `@11ty/eleventy-img` (responsive resizing at build time)
- **Templating:** Nunjucks
- **Styling:** Hand-written CSS (no framework)
- **Source of truth for content:** Markdown + a single YAML data file
- **Hosting:** Plain web server (static files), source kept in git, no runtime needed

## Project structure

```
canvas_man_website/
├── site_files/                   # Original Joomla files + DB (reference only)
├── build/                        # Intermediate extraction data (gitignored)
├── src/                          # Eleventy source
│   ├── _data/
│   │   └── site.yml              # Business info: name, address, phone, hours, email, nav
│   ├── _includes/
│   │   ├── layouts/
│   │   │   ├── base.njk          # HTML shell, head, body, header, footer
│   │   │   ├── page.njk          # Standard content page
│   │   │   └── gallery.njk       # Gallery category page
│   │   └── partials/
│   │       ├── header.njk
│   │       ├── footer.njk
│   │       └── nav.njk
│   ├── assets/
│   │   ├── css/site.css
│   │   └── js/lightbox.js
│   ├── images/
│   │   ├── content/              # Logos, supplier marks, hero images
│   │   └── gallery/
│   │       ├── residential/{patio-awnings,pool-cabana-covers,island-and-bbq-covers,protect-covers,window-awnings,enclosures}/
│   │       ├── commercial/{awnings,gym-wall-mats,truck-covers,protect-covers}/
│   │       └── marine/{center-consoles,bimini-tops-and-enclosures,camper-backs,cockpit-covers,convertible-tops,winter-covers,aft-covers,upholstery,enclosures,protect-covers}/
│   ├── index.md                  # Home
│   ├── about.md
│   ├── contact.md
│   ├── residential/index.md
│   ├── commercial/index.md
│   ├── marine/index.md
│   └── gallery/index.njk         # Auto-paginates one page per category
├── .eleventy.js
├── package.json
├── .gitignore
└── README.md
```

## Site map / navigation

- **Home**
- **Residential** — landing + 6 gallery categories
- **Commercial** — landing + 4 gallery categories
- **Marine** — landing + 10 gallery categories
- **About Us**
- **Contact**

## Gallery taxonomy

Sub-galleries from the Joomla FW Gallery are flattened into top-level categories per area for a simpler UX.

| Area | Categories | Image count |
|---|---|---|
| Residential | Patio Awnings, Pool Cabana Covers, Island & BBQ Covers, Protect Covers, Window Awnings, Enclosures | 200 |
| Commercial | Awnings, Gym Wall Mats, Truck Covers, Protect Covers | 25 |
| Marine | Center Consoles, Bimini Tops & Enclosures, Camper Backs, Cockpit Covers, Convertible Tops, Winter Covers, Aft Covers, Upholstery, Enclosures, Protect Covers | 190 |
| **Total** | **20 categories** | **415 images** |

## Build phases

1. **Phase 1 — Extract & organize assets** ✅
   - Parse FW Gallery DB to map image filenames → categories
   - Extract only original-resolution images from tar
   - Place them under `src/images/gallery/<area>/<category>/`

2. **Phase 2 — Eleventy scaffolding** ✅ (in progress)
   - `package.json`, `.eleventy.js`, `.gitignore`
   - Add `@11ty/eleventy-img` for responsive images

3. **Phase 3 — Layouts & styling**
   - `base.njk`, header, footer, nav partials
   - Clean modern responsive CSS

4. **Phase 4 — Content**
   - `_data/site.yml` with all editable business info
   - Markdown pages for Home, About, Contact, Residential, Commercial, Marine
   - Pull descriptions from DB (`jos_fwg_projects.description`)

5. **Phase 5 — Gallery system**
   - Eleventy data file walks `images/gallery/` and emits one page per category
   - Lightbox JS for image viewing
   - Responsive thumbnails generated by `eleventy-img`

6. **Phase 6 — Build, verify, deploy**
   - `npm install && npm run build`
   - Smoke-test in browser
   - Output: `_site/` directory ready to rsync to any web host

## How content updates work post-build

| To change... | You edit... |
|---|---|
| Address, phone, hours, email | `src/_data/site.yml` |
| Page text | `src/<page>.md` |
| Add gallery photos | Drop into `src/images/gallery/<area>/<category>/` |
| Add new gallery category | Make a new folder under `src/images/gallery/<area>/` |
| Add a new page | Create `src/<name>.md` and add to nav in `site.yml` |

## Image curation backlog

Deferred curation passes — do these before publishing.

- [x] **Image variant upgrade** — initial extraction grabbed display-sized variants (suffixed `-2`, `-3`, etc.) for 38 images. Replaced each with the highest-resolution version available in the original tar.
- [x] **Exact duplicate removal** — removed 16 same-category byte-identical dupes. 4 cross-category dupes intentionally preserved (one photo legitimately illustrates two categories).
- [x] **Low-resolution archive** — moved 24 images with min-dimension < 500px to `archive/low-resolution/` (preserving directory structure) with a documentation log at `archive/ARCHIVE_LOG.md`. Files are not deleted; they can be restored by moving back into `src/images/gallery/`. **375 images now live.**
- [x] **Empty-category report (automatic)** — the gallery data loader detects category folders with no images and writes `archive/EMPTY_CATEGORIES.md` on every build. Empty categories are silently skipped from the site (no page, no listing) so they don't show as broken pages. The report lists each empty folder and notes whether any of its images were archived, so it's easy to restore or remove.
- [ ] **Perceptual near-duplicate detection** — find images that are visually similar but not byte-identical (different sizes, slight crops, re-encodes of the same shot). Use a perceptual hash (pHash / dHash) with a similarity threshold. Trickier than exact dedup because thresholds need tuning.
- [ ] **Manual contact-sheet review** — generate a single HTML page per category showing every photo as a thumbnail with a checkbox or keep/cut toggle. Lets a human scan ~50 photos in seconds and produce a curated list. Useful for the final pass after automated filters.

## Design / UX backlog

- [ ] **Top-bar nav has too many items and wraps awkwardly** — with Home / Residential / Commercial / Marine / Canvas Services / FAQ / About / Contact, "Canvas Services" breaks across two lines on desktop because the right-aligned nav runs out of room. Plus, eight top-level links is a lot of cognitive load. Options to evaluate:
  - **Consolidate** — drop About into a footer link or merge Canvas Services into Residential/Marine area pages.
  - **Shorter labels** — "Canvas Services" → "Services"; "Frequently Asked Questions" → "FAQ" (already shorter).
  - **Two-row header** — primary nav on one row, secondary (About, FAQ, Contact) on a second.
  - **Hamburger sooner** — switch to the mobile menu at a wider breakpoint (e.g. < 1100px instead of < 800px) so the nav collapses before it wraps.
  - **`nowrap` on link labels** — prevents multi-word labels like "Canvas Services" from breaking mid-label; doesn't fix the total-width problem but stops the worst visual artifact.

  Defer until after the live site cutover so we don't churn the structure while validating content.

## Tooling / DX backlog

- [ ] **Surface the empty-category report during build** — right now `archive/EMPTY_CATEGORIES.md` is regenerated silently on every build. Hook it into the build flow so empty categories show up where they'll actually be seen. Options to evaluate:
  - **Console warning at build end.** Wrap `npm run build` with a script (or use Eleventy's `eleventy.after` event) that prints `⚠️ 1 empty category — see archive/EMPTY_CATEGORIES.md` after the build summary. Lowest-friction.
  - **Eleventy `--quiet` summary line.** Use `eleventyConfig.on("eleventy.after", ...)` to add a one-line status to Eleventy's own output so it sits alongside `Wrote 25 files`.
  - **Pre-deploy / CI gate.** If deploying through CI, fail the build (or post a PR comment) when empty categories appear unexpectedly. Useful once a deploy pipeline exists.
  - **Sidebar banner on `/contact/` or a hidden `/build-report/` page.** A page on the site itself listing build-time warnings — visible only when accessed directly. Heavier than the console option.

  Recommended next step: start with the console warning hook (smallest change, no new pages). Revisit a build-report page once there are more build-time signals worth showing (broken links, missing alt text, etc.).

## Content ported

- [x] **Home page copy** — tagline, hero, and body adapted from the live canvasman.com so the new site matches existing brand positioning ("Long Island's leading provider...", "Established 1972", "a true custom shop").
- [x] **FAQ** — all 19 Q&A entries from `canvasman.com/faq` ported into `src/_data/faq.yml` and rendered at `/faq/` as a no-JS `<details>` accordion. Includes the full 36-row stain-removal table.
- [x] **About page** — pulls real founding year (1972) and region ("Long Island") from `site.yml`.
- [x] **Canvas Services page** — ported from `canvasman.com/canvas-services.html`. Four sections: intro, Marine Canvas Repairs, Canvas Awning Repairs, Canvas Awning Service. Legacy anchors (`#marine-canvas-repairs`, `#marine-awning-repairs`, `#canvas-awning-services`) preserved so deep links from the old site still work. The intro CTA was rewritten to pull the phone number from `site.yml` (replacing the outdated "Give Mike or Tom" text).
- [ ] **Q1 FAQ wording** — answer says "from Canvas Man" verbatim (matches live source). Decide whether to keep "Canvas Man" as a brand reference or substitute the formal name "Perino's Canvas & Awnings". Easy `sed` either way.

## Open questions

These were deferred during planning. They don't block the initial build but should be answered before publishing.

2. **Visual design reference** — The original Joomla site uses the default `rhuk_milkyway` template (~15 years old). Going with a clean modern design rather than mirroring the old look. Confirm this preference, or share a reference site you want to emulate.

3. **Business info accuracy** — Pulled from the Joomla DB (`jos_contact_details`):
   - Name: Perino's CanvasMan
   - Address: 154 W. Main St., Bay Shore, NY 11706
   - Phone: (631) 581-5168
   - Fax: (631) 581-3291
   - Email: thomas@canvasman.com

   Confirm these are current, or provide updates. Also need: **opening hours** (not in DB).

4. **Contact form** — The Joomla site had a contact form via `jos_contact_details`. A static site can't process forms by itself. Options:
   - Plain `mailto:` link (simplest, no JS)
   - Third-party form handler (Formspree, Basin, Netlify Forms) — needs a free account
   - No form, just phone + email + address shown

5. **Domain & deployment** — Where will the new site live? Replacing canvasman.com directly, a staging subdomain first, or somewhere else? Affects only the deploy step.

6. **SEO & redirects** — The old site has 7,000+ entries in `jos_sh404sef_aliases` (URL rewrites). If we replace canvasman.com directly, we may want to set up 301 redirects from common old URLs to new ones to preserve search ranking. Out of scope for v1 but worth noting.
